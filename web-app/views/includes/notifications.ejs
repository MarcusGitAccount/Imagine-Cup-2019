<% let messages = null %>
<% if (locals.getMessages) { %>
<%  messages = getMessages() %>
<% } %>

<% if (messages && messages.error && Array.isArray(messages.error)) { %>
  <% for (const type in messages) { %>
    <% for (const message of messages[type]) { %>
      <p class=<%- type %>><%- message %></p>
    <% } %> 
  <% } %>
<% } %>  